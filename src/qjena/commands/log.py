from __future__ import annotations

from qlever.commands import log as log_command


class LogCommand(log_command.LogCommand):
    """
    Should behave exactly the same as log command in qlever
    """
